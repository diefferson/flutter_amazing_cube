import 'package:flutter/material.dart';
import 'package:flutter_amazing_cube/cube.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return CubeWidget();
  }
}
